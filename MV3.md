# MV3 Checklist

[x] Serialize storage [x] Browser actions [ ] Remove DOM usage from background [ ] Replace timers with alarms [ ] Update the manifest [ ] Inject into pre-existing tabs. tabs.executeScript? Do we need this with MV3?
